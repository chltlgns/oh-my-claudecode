# BiniLab v3.2 — Phase별 구현 프롬프트

> 각 Phase를 새 세션에서 실행할 때 이 프롬프트를 복사해서 붙여넣기.
> 반드시 handoff.md를 먼저 읽고 시작.

---

## Phase 0: 기존 버그 정리 (30분)

```
BiniLab v3.2 Phase 0을 실행해줘.

목표: 기존 버그 정리 + CI 기준선 복구

작업 목록:
1. safety-gates.test.ts에서 gate6_qaPassCheck 테스트 수정
   - gates.ts:137에서 임계값이 10→6으로 변경됨
   - 테스트는 아직 gate6_qaPassCheck(9)가 false를 기대하고 있음
   - 9 >= 6 = true이므로 테스트를 수정해야 함
   - npm test 실행 → 348/348 통과 확인

2. COMPANY.md 워밍업 수치 업데이트
   - "7/20" → "19/20"으로 변경

3. stale 전략 문서 정리
   - PLAN-AI-COMPANY.md, PLAN-COMPANY-V2.md, PLAN-COMPANY-V2-FINAL.md, PLAN-DB-CLEANUP.md
   - 각 파일 최상단에 "<!-- DEPRECATED: COMPANY.md가 SSOT -->" 추가
   - 내용은 삭제하지 않음 (참고용 유지)

검증: npm test → 348/348 통과
```

---

## Phase 0.5: 수익 모델 검증 (1세션)

```
BiniLab v3.2 Phase 0.5를 실행해줘.

목표: Threads에서 쿠팡 파트너스 제휴링크가 기술적으로 작동하는지 1건 테스트

현재 상태:
- 워밍업 19/20 (1개 남음)
- 제휴링크 0개 게시
- 수익 ₩0

작업 목록:
1. 워밍업 완료
   - content_lifecycle에서 미게시 포스트 1개 확인
   - /threads-post로 게시 → 20/20 달성

2. 쿠팡 파트너스 링크 생성
   - 성과 TOP 포스트(목이버섯 15K뷰, 도시락 8.7K뷰)와 관련된 제품 검색
   - 쿠팡 파트너스 대시보드에서 딥링크 생성 방법 조사
   - 실제 딥링크 1개 생성

3. 테스트 3가지 방식 (순서대로 시도)
   - A: 기존 고성과 포스트에 댓글로 링크 삽입
   - B: 새 포스트 본문에 링크 포함
   - C: 프로필 bio에 링크 + 포스트에서 "프로필 링크 확인" 유도
   - Chrome CDP로 실행

4. 24h 후 확인 (다음 세션)
   - 쿠팡 파트너스 대시보드에서 클릭 확인
   - Threads 포스트 도달 비교 (링크 포함 vs 미포함)

5. 결과 기록
   - content_lifecycle에 revenue 관련 필드 업데이트
   - handoff.md에 검증 결과 기록

검증 기준:
- 클릭 1건+ → "모델 작동" 판정, Phase 1 진행
- 클릭 0 + 도달 페널티 → 대안 검토 기록

참고: CLAUDE.md의 에이전트 스폰 규칙, 에이전트 표기 규칙(이름(직책)) 준수
```

---

## Phase 1: 학습 루프 B+C+D+E (1세션, TDD)

```
BiniLab v3.2 Phase 1을 실행해줘.

목표: processAgentOutput()을 프로덕션에 연결. 에이전트 기억 읽기+쓰기 모두 자동화.

현재 문제:
- processAgentOutput() (agent-output-parser.ts:101) — 프로덕션 import 0건
- loadAgentContext() (memory.ts) — 자동 작동 중 ✅
- 기억 읽기는 자동, 쓰기는 수동 → 학습 루프 단절

작업 목록 (TDD 적용):

Step 1: TDD — saveAgentResponse() 테스트 먼저
  - src/__tests__/agent-spawner.test.ts에 새 테스트 추가
  - 테스트: saveAgentResponse(agentId, output) 호출 시
    - [SAVE_MEMORY] 태그 파싱 → agent_memories INSERT
    - [LOG_EPISODE] 태그 파싱 → agent_episodes INSERT
    - 태그 없으면 status: 'missing_tags' 반환
  - 기존 agent-output-parser.test.ts의 mock 패턴 참조
  - npm test → 새 테스트 실패 확인 (아직 함수 없으니까)

Step 2: D — agent-spawner.ts에 saveAgentResponse() 구현
  - processAgentOutput() import from agent-output-parser.ts
  - 래퍼 함수:
    export async function saveAgentResponse(
      agentId: string,
      output: string
    ): Promise<ProcessResult> {
      return processAgentOutput(agentId, output);
    }
  - npm test → 새 테스트 통과 확인

Step 3: B — /analyze-performance 스킬에 기억 저장 추가
  - ~/.claude/skills/analyze-performance/SKILL.md 수정
  - Step 4 (리포트 DB 저장) 이후에 Step 4.5 추가:
    saveMemory({
      agentId: 'seoyeon-analyst',
      scope: 'analytics',
      memoryType: 'insight',
      content: '분석 핵심 인사이트',
      importance: 0.7,
      source: 'analyze-performance'
    })
  - 임시 스크립트 패턴으로 작성

Step 4: E — PostToolUse 훅 등록
  - src/hooks/auto-parse-agent.ts 신규 생성
  - 에이전트 응답에서 [SAVE_MEMORY], [LOG_EPISODE] 태그 자동 파싱
  - ~/.claude/settings.json 또는 .claude/settings.json에 훅 등록:
    PostToolUse > matcher: "Agent" > command: auto-parse-agent.ts

Step 5: 통합 검증
  - 서연(애널리스트) Agent() 스폰 (기억 주입 포함)
  - 분석 수행 지시
  - [SAVE_MEMORY] 태그가 응답에 포함되는지 확인
  - DB에 agent_memories에 새 기록 생성 확인
  - agent_memories 2건 → 3건+ 확인

주의사항:
- C (CLAUDE.md 규칙)는 이미 추가됨 ✅
- agent_episodes 테이블은 created_at이 아니라 occurred_at 사용
- processAgentOutput 시그니처: (agentId: string, output: string) → ProcessResult
- enforceTagGate 시그니처: (agentId: string, output: string, retryFn) → string

검증: npm test 전체 통과 + agent_memories 건수 증가 확인
```

---

## Phase 2-A: 상주 에이전트 2명 (CEO + 서연) (1세션)

```
BiniLab v3.2 Phase 2-A를 실행해줘.

목표: 민준(CEO) + 서연(애널리스트) 2명을 별도 Claude Code 세션으로 상주 시작

전제조건: Phase 1 완료 (학습 루프 작동 확인)

작업 목록:

1. Bun 설치
   curl -fsSL https://bun.sh/install | bash
   → which bun 확인

2. claude-peers 설치 + MCP 등록
   git clone https://github.com/louislva/claude-peers-mcp ~/claude-peers-mcp
   cd ~/claude-peers-mcp && bun install
   claude mcp add --scope user --transport stdio claude-peers -- bun ~/claude-peers-mcp/server.ts

3. _load-memory.ts를 프로젝트 루트에 영구 보관 (삭제하지 않음)
   - 에이전트 스폰 시 기억 로드용

4. tmux 세션 생성
   tmux new-session -d -s binilab -n main
   tmux new-window -t binilab -n ceo
   tmux new-window -t binilab -n seoyeon

5. 각 에이전트 시작 프롬프트 작성
   - CLAUDE.md 읽기 지시
   - 에이전트 정의 파일 읽기
   - 기억 주입 (loadAgentContext 결과)
   - RELOAD 규칙 포함
   - "대기 중" 출력 후 지시 대기

6. 검증
   - CEO 세션: list_peers() → 서연 발견
   - CEO: send_message(서연, "성과분석해")
   - 서연: /analyze-performance 실행
   - 서연: [SAVE_MEMORY] → DB 저장 확인
   - agent_memories에 서연 기억 추가 확인

7. 변경 동기화 훅 설정 (Layer 1)
   - PostToolUse:Edit 훅 → peers RELOAD 메시지 전송
   - 테스트: CLAUDE.md 수정 → 서연 세션에 RELOAD 도착 확인

확대 기준: 서연(애널리스트) agent_memories 5건 이상 → Phase 2-B 진행
```

---

## Phase 2-B: 빈이(뷰티 에디터) + 도윤(QA) 추가

```
BiniLab v3.2 Phase 2-B를 실행해줘.

목표: 빈이(뷰티 에디터) + 도윤(QA)을 상주 에이전트로 추가 (총 4명)

전제조건: Phase 2-A 완료, 서연 기억 5건+ 확인

작업:
1. tmux 윈도우 추가
   tmux new-window -t binilab -n bini
   tmux new-window -t binilab -n doyun

2. 빈이 시작 (기억 주입 + 페르소나 + 글쓰기 가이드)
3. 도윤 시작 (기억 주입 + QA 체크리스트)

4. 검증
   - CEO → 빈이에게 포스트 요청
   - 빈이 작성 → 도윤 QA → PASS/REJECT
   - 양쪽 [SAVE_MEMORY] → DB 저장 확인

확대 기준: 빈이+도윤 기억 각 3건+ → Phase 2-C
```

---

## Phase 2-C: 준호(리서처) + 태호(엔지니어) 추가

```
BiniLab v3.2 Phase 2-C를 실행해줘.

목표: 준호(리서처) + 태호(엔지니어) 추가 (총 6명 상주 완성)

작업:
1. tmux: binilab:junho, binilab:taeho
2. 준호 시작 (수집 도구 목록 + 기억)
3. 태호 시작 (인프라 도구 + 기억)
4. 전체 6명 peers 연결 확인: CEO에서 list_peers() → 5명 표시

6명 상주 + 5명 온디맨드 구조 완성.
```

---

## Phase 2.5: 자산 활용률 개선 (1~2세션)

```
BiniLab v3.2 Phase 2.5를 실행해줘.

목표: 수집→콘텐츠 전환율 1.5% → 10%+ 개선

현재 미활용 자산:
- youtube_videos: 85개 → 콘텐츠 전환 0건
- brand_events: 7개 유효 (토리든/일동/샤오미/다이슨/무인양품) → 사용 0건
- trend_keywords: 847개 (selected 7) → 콘텐츠 전환 0건
- 벤치마크 1,215개 → 25 콘텐츠 (2.1%)

작업 목록:

1. YouTube 데이터 활용 파이프라인
   - /daily-run Phase 1.5 _signal-scan.ts에 youtube_videos TOP 10 추가
   - SELECT title, view_count, channel_id, comments FROM youtube_videos
     ORDER BY view_count DESC LIMIT 10
   - 서연(애널리스트) 분석 시 YouTube 교차 검증:
     "YouTube 조회수 + 벤치마크 조회수 동시 상위 = 기회점수 +3"
   - 빈이(에디터) 포스트에 "유튜브 XX만뷰짜리" 팩트 인용

2. brand_events 활용 파이프라인
   - /daily-run Phase 1.5에 brand_events 유효 목록 추가
   - SELECT event_id, brand_id, title, event_type, urgency, expires_at
     FROM brand_events WHERE is_stale = false AND is_used = false
   - 민준(CEO) directive에 "브랜드 이벤트 슬롯" 배정
   - 포스트 작성 후 UPDATE brand_events SET is_used = true WHERE event_id = ?

3. trend_keywords 활용
   - selected=true 키워드 → collect-by-keyword.ts로 Threads 검색 수집
   - 수집 결과를 thread_posts에 저장 (post_source='x_trend')
   - 콘텐츠 파이프라인에 투입

4. 리사이클 시스템 연결
   - recycle.ts가 이미 구현되어 있음
   - 고성과 포스트(15K뷰 목이버섯 등)의 주제를 변형하여 재생산
   - 14일 중복 방지 규칙 준수

검증:
- youtube_videos → 콘텐츠 전환 10건+
- brand_events is_used=true 3건+
- 수집→콘텐츠 전환율 10%+ (1,659 → 165건 목표)
```

---

## Phase 3: Pixel Agents 시각화 (30분)

```
BiniLab v3.2 Phase 3을 실행해줘.

목표: VS Code에서 에이전트 캐릭터 시각화

작업:
1. Pixel Agents VS Code 확장 설치
   code --install-extension pixel-agents.pixel-agents

2. VS Code settings.json에 BiniLab 에이전트 매핑
   6명 상주 에이전트 캐릭터 설정

3. 확인: 각 tmux 터미널이 VS Code에서 캐릭터로 표시되는지

API 비용 $0. WSL2 환경에서 작동 확인 필요.
```

---

## Phase 4: 대시보드 (수익 발생 후)

```
BiniLab v3.2 Phase 4를 실행해줘.

진입 조건: 제휴링크 클릭 100건+ 또는 첫 수익 ₩1,000+ 발생

목표: Next.js + Supabase Realtime 대시보드

작업:
1. Next.js 15 프로젝트 생성 (App Router + Tailwind + shadcn/ui)
2. Supabase JS Client 연동 (Realtime 구독)
3. 페이지 구현:
   - / : 에이전트 상태 + agent_messages 실시간 + 파이프라인 진행률
   - /performance : 포스트 성과 차트 + 카테고리별 ROI
   - /agents : 에이전트별 기억/에피소드 뷰어
   - /revenue : 수익 추적 (클릭/전환/수익)
4. Vercel Free tier 배포

API 비용 $0 (DB 읽기 전용, LLM 호출 없음)
```

---

## 사용 방법

1. 새 Claude Code 세션 시작
2. `handoff.md` 읽기 (세션 시작 규칙)
3. 위 프롬프트 중 해당 Phase 복사 → 붙여넣기
4. 완료 후 handoff.md 업데이트 + 커밋
